<%- include('partials/header') %>

<div class="container" style="padding-top: 150px; padding-bottom: 80px;">
  <h1 class="mb-4">Спасибо за заказ!</h1>

  <p>Номер заказа: <strong><%= order.id %></strong></p>
  <p>Дата: <%= order.date %></p>
  <p>Имя: <%= order.name %></p>
  <p>Телефон: <%= order.phone %></p>
  <p>Адрес: <%= order.address %></p>
  <h5>Сумма заказа: <%= order.total %> руб.</h5>

  <a href="/products" class="boxed-btn3 mt-4 d-inline-block">Вернуться в каталог</a>
</div>

<%- include('partials/footer') %>